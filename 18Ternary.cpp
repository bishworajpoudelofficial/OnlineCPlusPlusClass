#include <iostream>

using namespace std;

int main(){
// Ternary operator syntax: condition ? write value ? wrong
int age = 20;

int a = (age ==20) ? a=1 : a=0;

cout<< "Value of a is "<<a;

return 0;
}

